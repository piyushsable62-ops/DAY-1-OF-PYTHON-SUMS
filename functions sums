
# functions
def avg():
    a = int(input("enter numbers : "))
    b = int(input("enter numbers : "))
    c = int(input("enter numbers : "))

    average = (a+b+c)/3
    return average
d = avg()
print(d)

# mulptiplication using fn 

def multiply(n):
    for i in range (1,11):
        print(f"{n} x {i} = {n*i}")
multiply(10)    
